export const selectUserName = (state) => state.userData.name;

export const selectUserGender = (state) => state.userData.gender;

export const selectUserWeight = (state) => state.userData.weight;

export const selectUserWaterRate = (state) => state.userData.waterRate;

export const selectUserSportTime = (state) => state.userData.sportTime;

export const selectUser = (state) => state.user.user;